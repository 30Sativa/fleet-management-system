#!/usr/bin/env bash
# TODO(WP3): replace this skeleton when digital-twin/ has source.
# Required contract: exit 0 on pass, non-zero on failure, and never hide errors.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

if [ ! -f package.xml ] && [ ! -f CMakeLists.txt ] && \
   [ ! -f package.json ] && [ ! -f pyproject.toml ]; then
  echo "SKIPPED: digital-twin/ has no source yet. Nothing to verify."
  exit 3
fi

echo "FAIL: digital-twin/ has source but scripts/verify is still a skeleton."
echo "      Replace the TODO with the real checks before adding source."
exit 1
