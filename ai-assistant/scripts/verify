#!/usr/bin/env bash
# TODO(WP4): replace this skeleton when ai-assistant/ has source.
# Required contract: exit 0 on pass, non-zero on failure, and never hide errors.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

if [ ! -f package.json ] && [ ! -f pyproject.toml ] && [ ! -f go.mod ]; then
  echo "SKIPPED: ai-assistant/ has no source yet. Nothing to verify."
  exit 3
fi

echo "FAIL: ai-assistant/ has source but scripts/verify is still a skeleton."
echo "      Replace the TODO with the real checks before adding source."
exit 1
